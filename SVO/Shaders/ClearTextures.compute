#pragma kernel CSMain

RWTexture2D<float> ResultDistance;
RWTexture2D<float4> Result;

[numthreads(16,16,1)]
void CSMain (uint3 id : SV_DispatchThreadID)
{
    ResultDistance[id.xy] = 1.#INF;
    Result[id.xy] = float4(0.f, 0.f, 0.f, 0.f);
}